import SwiftUI

// Wrapper that provides the SwapTokenSheet implementation
// Uses the Enhanced version which includes all animations and improved UX
struct SwapTokenSheet: View {
    var body: some View {
        SwapTokenSheet()
    }
}
