import SwiftUI

// Wrapper that provides the SendTokenSheet implementation
// Uses the Enhanced version which includes all animations and improved UX
struct SendTokenSheet: View {
    var body: some View {
        SendTokenSheet()
    }
}
