platform :ios, '16.0'

target 'Interspace' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # All SDKs moved to Swift Package Manager due to CocoaPods compatibility issues with Xcode 16.3
  # pod 'GoogleSignIn'
  # pod 'metamask-ios-sdk'
  # pod 'CoinbaseWalletSDK', '1.0.3'
  # pod 'reown-swift'
  # QR Code scanning - using iOS native AVFoundation instead of external dependency

end
